# Inference-only dependencies for the deployed function.
#
# LiteRT rather than tensorflow-cpu: the full package installs to ~1.4 GB and
# Vercel rejects any function over 500 MB. LiteRT only runs models, which is
# all this endpoint does, and installs in single-digit MB.
#
# The training requirements (pandas, scikit-learn, matplotlib, seaborn,
# kaggle) are in the repository root requirements.txt and are not needed
# to serve predictions.
ai-edge-litert>=1.0
fastapi>=0.110
python-multipart>=0.0.9
pillow>=10.0
numpy>=1.26
