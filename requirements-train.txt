# Training and evaluation dependencies — for running src/dermai locally or
# on Kaggle. Deliberately not named requirements.txt: that name is claimed by
# the deployed function's dependency list, and putting full TensorFlow there
# pushed the Vercel bundle to 2229 MB against a 500 MB limit.
tensorflow>=2.19
pandas>=2.0
numpy>=1.26
scikit-learn>=1.4
matplotlib>=3.8
seaborn>=0.13
Pillow>=10.0
kaggle>=1.6
